# 2. Exercise. Conditionals Simple
# Alter the code demonstrating how to make use of the built-in python function `print()`
# only when certain variables match the required conditions with the use of `if` and `then`.

product = "Milk"
price = 3
currency = "USD"
dateofproduction = 1767818536.745559

# Alter the code below.
# - Print the product name ONLY if it's `Milk`
# - Print the price, ONLY if the price is less than 4.0

print(product)
print(price)